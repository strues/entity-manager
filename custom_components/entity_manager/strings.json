{
  "config": {
    "step": {
      "user": {
        "title": "Entity Manager",
        "description": "Set up Entity Manager to enable entity management services (enable, disable, rename, bulk rename)."
      }
    },
    "abort": {
      "already_configured": "Entity Manager is already configured."
    }
  }
}

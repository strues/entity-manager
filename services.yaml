enable_entity:
  name: Enable Entity
  description: Enable a disabled entity through Entity Manager
  fields:
    entity_id:
      name: Entity ID
      description: The entity ID to enable
      required: true
      example: "sensor.my_sensor"
      selector:
        text:

disable_entity:
  name: Disable Entity
  description: Disable an enabled entity through Entity Manager
  fields:
    entity_id:
      name: Entity ID
      description: The entity ID to disable
      required: true
      example: "sensor.my_sensor"
      selector:
        text:

<<<<<<< Updated upstream
rename_entity:
  name: Rename Entity
  description: Rename an entity's friendly name and/or entity ID
  fields:
    entity_id:
      name: Entity ID
      description: The current entity ID to rename
      required: true
      example: "sensor.my_sensor"
      selector:
        text:
    name:
      name: New Name
      description: The new friendly name for the entity
      required: false
      example: "Kitchen Temperature"
      selector:
        text:
    new_entity_id:
      name: New Entity ID
      description: The new entity ID (e.g. sensor.kitchen_temp)
      required: false
      example: "sensor.kitchen_temp"
      selector:
        text:
=======
bulk_rename:
  name: Bulk Rename
  description: Rename multiple entities in the entity registry
  fields:
    renames:
      name: Renames
      description: List of rename operations (entity_id -> new_entity_id)
      required: true
      example:
        - entity_id: sensor.old_name
          new_entity_id: sensor.new_name
      selector:
        object:
          multiple: true
    dry_run:
      name: Dry Run
      description: If true, only log planned renames
      required: false
      default: false
      selector:
        boolean:
    skip_missing:
      name: Skip Missing
      description: If true, skip entities that are not found
      required: false
      default: true
      selector:
        boolean:
>>>>>>> Stashed changes

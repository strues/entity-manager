language: "en"
intents:
  entity_manager_enable_entity:
    data:
      - sentences:
          - "enable entity {entity}"
          - "enable the entity {entity}"
          - "activate entity {entity}"
          - "registry enable {entity}"
  
  entity_manager_disable_entity:
    data:
      - sentences:
          - "disable entity {entity}"
          - "disable the entity {entity}"
          - "deactivate entity {entity}"
          - "registry disable {entity}"
